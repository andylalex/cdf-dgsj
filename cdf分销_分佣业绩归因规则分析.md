# cdf 海南免税分销 PRD — 分佣业绩归因规则分析

> 来源：`cdf海南免税分销产品需求文档V1.0.0.docx`
> 核心规则集中在 **「员工分享商品返佣说明」**（P106 起）的「返佣分配需求说明」「佣金计算需求说明」，以及「订单管理-返佣状态说明」（P1157）、「业绩分析-金额验算」（P1594）。

---

## 一、业绩归因的判定机制（订单归给哪个分销员工）

原文（P115 返佣分配需求说明，P116–P146）：

- **归因参数**：用户通过分销链接进入商品详情页时，分销系统传给商城：`"商品ID、员工ID、绑定有效时间分钟数"`。
  > 原文 P116：「当终端下单用户，通过分销链接，进入商城对应商品详情页时，分销系统将传给商城相关参数包括："商品ID、员工ID、绑定有效时间分钟数"。」
  > 原文 P117（示例）：「12345、Y0029、绑定时间60分钟。」
- **归因载体 = 点击行为日志**：商城记录用户与分销员工通过商品产生的所有"点击行为"日志。
  > 原文 P119：「商城需要记录，用户与分销员工，通过商品产生的所有"点击行为"日志数据。」
  > 原文 P120（示例）：「A用户，在2022-1-30 18:39:34，通过员工A，员工ID001，进入商品A。」
  > 原文 P121：「(包括同一用户通过同一个员工，对同一商品的多次点击行为)」
- **有效绑定时间判定**：以"最新一条点击行为"计算有效绑定时间，在有效绑定时间内所下有效订单均计算返佣。
  > 原文 P123：「商城需要通过有效绑定时间，判断是否推送分销系统订单：下单用户与分销员工，存在未过期的有效绑定时间。」
  > 原文 P143：「以最新一条"点击行为"，计算用户与员工的有效绑定时间，在有效绑定时间内，所下的有效订单，均计算返佣。」
- **归因推送内容**：推送分销系统的订单须包含订单基础信息、用户与所有员工的点击行为日志、用户与每个员工的绑定时间分钟数。
  > 原文 P127–P130：「推送分销系统订单时，需要包含以下数据：订单基础信息。用户与所有员工产生"点击行为"的日志数据。用户与每个员工的绑定时间分钟数。」
- **跨端归因**：临时设备号与账号绑定后，H5/小程序/Android/iOS 各平台返佣订单均推送分销系统。
  > 原文 P146：「当用户临时设备号与账号绑定后，在电商各平台（包括：H5、小程序、Android、IOS）下的返佣订单，均推送至分销系统。」

**小结**：业绩归因以「**员工ID + 商品点击行为 + 有效绑定时间**」为三要素；一个订单只要含任一由分销系统产生的点击行为，整单都被推送归因（P144），但绑定过期则不归因。

---

## 二、归因失效 / 无效订单规则（不计入业绩）

原文（P132–P138 订单过滤校验）：

> P133：「需要校验订单中用户与员工的绑定时间是否过期，当订单中用户绑定时间过期，则此类订单不做保存。」
> P134：「当企业冻结时，订单返佣状态状态标记为"无效订单-企业冻结"。此订单不参与后续结算，且在分销端订单中不展示，也不统计相关业绩收益数据。」
> P135：「当员工冻结时，订单返佣状态状态标记为"无效订单-员工冻结"。此订单不参与后续结算，且在分销端订单中不展示，也不统计相关业绩收益数据。」
> P136：「当订单中商品此刻在分销系统中全部都不存在，订单返佣状态标记为"无效订单-商品未同步"。此订单后续不参与结算」
> P137：「当订单中商品此刻在分销系统中全部都未配置返佣比例，订单返佣状态标记为"无效订单-商品未同步"。此订单后续不参与结算」
> P138：「当订单中商品此刻由此刻不存在的商品，及此刻未配置返佣比例的商品所组成，则订单返佣状态标记为"无效订单-商品未同步"。此订单后续不参与结算」

其他说明（P141–P142）：
> P142：「商品在分销平台或电商平台下架，仍然参与返佣计算，且不影响返佣状态。」（即下架不影响归因）

---

## 三、分佣计算规则（归因后算多少佣金）

原文（P150 佣金计算需求说明，P154–P162）：

- 只有通过过滤的有效订单才计算佣金；下架商品仍参与。
  > P154：「订单通过过滤机制校验，则置为有效订单，有效订单才能进行佣金的计算，商品在分销平台或电商平台下架，仍然参与返佣计算。」
- **每个商品匹配唯一返佣组**，按覆盖类型优先级：商品 > 品牌 > 品类。
  > P156：「查询出商品所归属，且未作废、未停用的返佣组。」
  > P157：「按照覆盖类型优先级，为商品匹配唯一的返佣组，1、商品 2、品牌 3、品类。」
- 无返佣 → 佣金为 0；未同步/未配置返佣比例 → 不参与计算。
  > P158：「当商品所属的返佣组，返佣比例被设为无返佣时，当前商品的返佣金额为"0"。」
  > P159：「订单中未同步至分销系统的商品，不参与佣金计算。」
  > P160：「订单中未配置返佣比例的商品，不参与佣金计算。」
- **计算公式**：实付金额 × 返佣比例，四舍五入保留两位小数；订单返佣金额 = 各商品佣金合计。
  > P161：「当商品所属的返佣组，返佣比例被设为有返佣时，则根据商品实付金额✖️返佣组比例 进行计算，计算的佣金四舍五入，保留两位小数。」
  > P162：「计算完订单中每个商品佣金额，进行总计，总计金额为订单返佣金额。」

---

## 四、业绩 / 返佣最终状态（归因结果的状态机）

原文（P1157 返佣状态说明，表 TBL1158）：

| 订单返佣状态 | 描述（原文） |
|---|---|
| 待返佣 | 用户下单成功，商品存在返佣金额。订单中至少存在一个商品下单时已同步，且下单时已配置返佣比例。 |
| 已返佣 | 待返佣的订单，用户提货/签收成功 |
| 无返佣-商品无返佣 | 用户下单成功，订单中商品均无返佣，此状态的返佣状态为最终状态，不会因为提货或结算而发生变化。 |
| 已结算 | 用户提货/签收成功，财务标记已打款 |
| 无效订单-已退货 | 订单退货成功 |
| 无效订单-企业冻结 | 用户下单成功，商城推送至分销系统，分销系统判断出当前企业已被冻结，此类订单不参与后续结算 |
| 无效订单-员工冻结 | 用户下单成功，商城推送至分销系统，分销系统判断出当前分销员工已被冻结，此类订单不参与后续结算 |
| 无效订单-商品未同步 | 当前订单中所有商品均在分销系统不存在，不参与后续结算；当订单中同时存在未同步和未配置返佣的商品，则返佣状态**优先取"无效订单-商品未同步"** |
| 无效订单-未配置商品返佣 | 当前订单中所有商品均未配置返佣比例，此类订单不参与后续结算 |

---

## 五、业绩统计口径（归因数据如何统计）

原文（P1594 业绩分析-金额验算，P1596–P1600）：

> P1597：「业绩分析中，所有的"件数"，对应着接口文档中，订单信息推送 - OrderItem - quantity（商品数量）。举例：订单中商品A数量为2；业绩统计中的件数也为2。」
> P1598（统计说明）：「各类 "销售" 数据中，不包括退款订单数据及无效订单数据。」

---

## 总览：分佣业绩归因逻辑链

1. **触点归因**：分销员工分享海报 → 用户点击进入商品详情页 → 记录「员工ID + 商品ID + 点击时间」点击行为，建立有效绑定（默认 60 分钟）。
2. **有效判定**：以**最新一次点击行为**为基准算有效绑定时间；有效绑定内下单 → 整单推送分销系统归因（含所有商品）。
3. **过滤失效**：绑定过期 / 企业冻结 / 员工冻结 / 商品未同步或未配置返佣 → 标记无效订单，不归因、不计业绩。
4. **分佣计算**：仅有效订单；每个商品按「商品>品牌>品类」优先级匹配唯一未作废未停用返佣组；佣金 = 实付金额 × 比例（两位小数）；订单佣金 = 各商品之和。
5. **状态落地**：待返佣 → 已返佣（提货/签收）→ 已结算；失败侧为各类"无效订单"；统计时剔除退款与无效订单。

---

## 六、绑定有效期是否可配置（补充追问）

**结论：在 cdf 海南免税商城侧，绑定有效期不是一个可配置项；它由分销系统（科玛分销平台）在用户点击分销链接时"传给商城"的参数。**

证据（原文逐句）：

- **下发方是分销系统，不是商城后台**：
  > P331：「当终端下单用户，通过分销链接，进入商城对应商品详情页时，分销系统将传给商城相关参数包括："商品ID、员工ID、绑定有效时间分钟数"。」
  > P332（示例）：「12345、Y0029、绑定时间60分钟。」
  > P1935（重复同样描述）：「当下单用户，通过分销链接，进入商城对应商品详情页时，分销系统将传给商城相关参数包括："商品ID、员工ID、绑定有效时间分钟数"。」
- **商城仅消费该值（用于过期校验 + 展示），不生产也不配置**：
  > P336–P337：「商城需要通过有效绑定时间，判断是否推送分销系统订单：下单用户与分销员工，存在未过期的有效绑定时间。」
  > P344：「需要校验订单中用户与员工的绑定时间是否过期，当订单中用户绑定时间过期，则此类订单不做保存。」
  > P3723–P3724：「绑定有效时间 / 展示下单用户与分销员工绑定有效时间，见科玛-分销平台接口文档-订单信息推送-validBindTime」—— 字段来源明确指向分销平台接口。
- **文档全部"可配置"项均与绑定有效期无关**：全文检索"可配置"出现约 60 处（P3811–P6508），全部是**小程序首页/分类/品牌馆/活动页/限时推广/消息等前端展示位**的配置（轮播图、金刚位、推广商品、跳转类型、翻页间隔等），**没有任何一处把"绑定有效期 / 绑定有效时间 / validBindTime"列为可配置项**。

**对结论的解读**：
1. cdf 海南免税商城：只读分销系统下发的 `validBindTime`，不构成商城后台配置点。
2. 分销平台（科玛）侧：PRD 只写"分销系统将传给商城"，未展开说明科玛后台是否提供该分钟数的配置入口——即**本 PRD（cdf 商城侧需求）未定义绑定有效期的配置能力**，若要支持可配，需在分销平台侧或双方约定中补充，否则默认按每次点击携带的固定值（示例 60 分钟）生效。
